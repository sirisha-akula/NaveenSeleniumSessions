package testngfeatures;

public class Notes {
	
	/**
	 * TestNG --> Java Unit testing framework/lib which is available in the form of jar files -- used by java dev/automation QA
	 * Phython --> PyTest, UnitTest
	 * PHP --> phpUnit
	 * JS --> Mocha, Jasmine
	 * C# --> NUnit
	 * 
	 * testcases:
	 * decorate tcs
	 * maintain tcs
	 * priority tcs
	 * depends on
	 * data provider -- test data
	 * parameterization 
	 * html report
	 * xml report 
	 * listeners -- extent/allure report 
	 * annotations -- preconditions/post steps /tcs
	 * assertions -- hard/soft
	 * test runner --  testng.xml
	 * 
	 * 
	 * src/main/java --> lib, util, page class
	 * src/test/java --> test classes --testng CODE
	 * 
	 * 
	 * verbose: gives testng logs in the console. the  min value  = 1, and max = 10, we need to write verbose = "2" in testng.xml file
	 * 
	 */

}
